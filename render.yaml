services:
  - type: web
    name: inmobiliaria-oriente
    runtime: python
    buildCommand: pip install -r requirements.txt && apt-get install -y poppler-utils
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
